import 'package:flutter/material.dart';

class Bar_item extends StatelessWidget {
  const Bar_item({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Bar item"),
      ),
    );
  }
}
